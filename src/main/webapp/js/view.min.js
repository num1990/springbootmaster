$(document).ready(function(){
    $("#rejectForm").validate({
        rules: {
            rejectReasonCode: {
                required: true
            },
            comments: {
                minlength: 4,
                maxlength: 200
            }
        },
        submitHandler: function (form) {
            form.submit();
        },
        messages: {
            rejectReasonCode:{
                required: "请选择拒绝原因"
            },
            comments: {
                minlength: "最少4个字",
                maxlength: "最多200字"
            }
        }
    });

    $("#rejectInternetForm").validate({
        rules: {
            rejectReasonCode: {
                required: true
            },
            comments: {
                minlength: 4,
                maxlength: 200
            }
        },
        submitHandler: function (form) {
            form.submit();
        },
        messages: {
            rejectReasonCode:{
                required: "请选择拒绝原因"
            },
            comments: {
                minlength: "最少4个字",
                maxlength: "最多200字"
            }
        }
    });

    $("#approveForm").validate({
        rules: {
            comments: {
                minlength: 4,
                maxlength: 200
            }
        },
        submitHandler: function (form) {
            form.submit();
        },
        messages: {
            comments: {
                minlength: "最少4个字",
                maxlength: "最多200字"
            }
        }
    });

});
